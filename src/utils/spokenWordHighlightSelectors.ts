/** Shared CSS selectors for spoken-word highlight spans (TTS + audiobook sync). */
export const SPOKEN_WORD_HIGHLIGHT_SELECTORS =
  ".tts-word-highlight, .tts-chunk-highlight, .audiobook-sync-word";
