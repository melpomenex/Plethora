/**
 * Re-export YouTubeViewer from the base component.
 * Note: Assistant panel is provided by DocumentViewerWrapper, so we don't add one here
 * to avoid duplicate panels when viewing YouTube videos.
 */
export { YouTubeViewer } from "./YouTubeViewer";
