# Sync documentation audit (queued)

The following files still describe the removed Yjs/CRDT sync subsystem and MUST be updated during OpenSpec Phase 7 (task 8.6):

- `docs/product/features/settings/encrypted-sync.md` — claims Yjs CRDT sync is implemented
- `src/features/help/generated/helpIndexData.ts` — help index references Yjs sync
- `docs/sync.prd` — canonical product requirements (correct; keep as source of truth)

Implementation tracker: `openspec/changes/implement-plethora-pro-sync-from-prd/`
