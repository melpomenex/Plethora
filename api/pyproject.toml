[project]
name = "incrementum-api"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["flit_core>=3.2"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "api"
