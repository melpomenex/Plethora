# Client-safe flags only. Never put secrets in PUBLIC_* or commit real .env files.
# Preview/staging defaults — production indexing is a launch-day flip.

PUBLIC_INDEXING=noindex
PUBLIC_DOWNLOADS_ENABLED=false
PUBLIC_CHECKOUT_ENABLED=false
PUBLIC_ANALYTICS_ENABLED=false
PUBLIC_DEMO_ENABLED=true
PUBLIC_COMMERCIAL_STOREFRONT_READY=false
PUBLIC_SITE_ORIGIN=https://useplethora.com
# PUBLIC_PLAUSIBLE_DOMAIN=
