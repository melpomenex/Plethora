<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      No special permissions required: we use the Storage Access Framework
      (ACTION_OPEN_DOCUMENT_TREE), which grants scoped access to the user's
      chosen folder without MANAGE_EXTERNAL_STORAGE. Staged copies land in the
      app's private filesDir.
    -->
    <application />
</manifest>
