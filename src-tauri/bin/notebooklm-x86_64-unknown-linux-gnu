#!/bin/sh
set -eu
SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
BUNDLED="$SCRIPT_DIR/notebooklm-runtime/x86_64-unknown-linux-gnu/.venv/bin/notebooklm"
if [ -x "$BUNDLED" ]; then
  exec "$BUNDLED" "$@"
fi
exec notebooklm "$@"
