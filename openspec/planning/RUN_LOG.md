# Plethora Transformation — Orchestration Run Log

Authority: `openspec/planning/plethora-transformation-roadmap.md`
Batch schedule: B0:1A | B1:1B | B2:{2,22a,24a} | B3:{3,5} | B4:{4,6,7-m1} | B5:{7-rest,8,9,13} | B6:{16,17,18,20} | B7:{19,10,12,14} | B8:{11,15,22-rest} | B9:{21,24-rest} | B10:{23}

Deep-review proposals (line-by-line diff review for data-loss / money / crypto / memory-budget before merge): 1-Phase-B, 4, 5, 6.

## Log

- 2026-08-17 (bootstrap): main @ 6881032c, all 24 proposals 0 tasks, no plethora/* branches. Committed the 24 proposals + roadmap to main (a48932c8) so worktrees see them. npm ci in main repo (env had no node_modules). Brand icon assets (6 files) remain untracked at repo root — staged into worktrees manually until task 2.4 lands them in `assets/brand/`.
- B0 — rebrand Phase A (tasks 1–2) merged dc94bc8e. 154 files: brand strings/i18n codemod (6 locales)/icons from assets/brand masters (zip verified byte-identical)/README-docs-CI-workflows/UA+Referer/extension display name; BRANDING.md + brandInventory.test.ts guardrail. Gates on main: test:run 3509✓, cargo 823✓, bench OK (1 stale-baseline info WARN), build OK. Decisions: AMO gecko id retained; plethora.app provisional domain; Android fresh-install+backup-import; handbook bodies swept (bundled ?raw imports are user-visible, DECISIONS D12); SW cache bumped incrementum-v8 (namespace kept till Phase B). Worktree/branch cleaned.
- B1 — rebrand Phase B (tasks 3–5) merged 3051e527. Identifier com.plethora.app + one-time desktop app-data migration (src-tauri/src/legacy_data.rs, crash-retry truncation self-healing); DB filename plethora.db with journal-protected legacy adoption; one-shot localStorage/IndexedDB key migration + SW plethora-v1; Keychain service rename with read-through + first-read migration; .plethora export + legacy .incrementum import + Obsidian id compat; dual-token app<->extension protocol with 1000-entry capped dedupe window; plugin crates rename (plethora-*); updater/checker/scripts re-anchored to Plethora repo + new minisign key; Android rotated keystore + backup paths; IncrementumError -> PlethoraError codemod (1,092 sites), crate/package names, PLETHORA_* env vars with legacy read fallback, MCP command aliases. Gates on main: test:run 3522✓, cargo 842✓, bench OK, build OK. Worktree/branch cleaned.
- B2 — Commercial product foundation & interface unblocking (Proposal 2 + 22a + 24a) merged on main. Capability registry (18 CapabilityIds, QuotaState, CapabilityState, EntitlementSnapshot) in TS & Rust (`src-tauri/src/entitlements/`); `EntitlementCache` with 72h offline grace resolution & local overrides; Tauri commands `entitlement_get_snapshot`, `entitlement_refresh`, `entitlement_override_set/clear`; `useEntitlementStore`, `useCapability`, `CapabilityGate`, `QuotaMeter`, `src/config/product.ts`; Settings v7 migration (`plethora.overrides`); `UserProfilePanel` snapshot consumption; Architecture invariant test (`architectureEntitlementsInvariant.test.ts`); capability display names in 6 locales; 22a privacy disclosure registry & `isCloudEligible` local-only shield + `docs/PRIVACY_ARCHITECTURE.md`; 24a cloud perf baselines schema & check script. Gates on main: test:run 3528✓, cargo 846✓, bench OK (21 benchmarks PASS, 0 regressions), build OK.
- B3 — Plethora Accounts, Authentication, Cloud Service & Job Quotas (Proposal 3 + 5) merged on main. Deep review completed for concurrency/SKIP-LOCKED/quotas. Server additive schema (`devices`, `sessions` with rotating refresh token families & reuse revocation, `jobs`, `usage_records`, `quota_state`, `capability_grants`); `/v1/auth/*`, `/v1/entitlements`, `/v1/jobs/*`, `/v1/usage` endpoints + error envelope; Rust client `plethora_auth` (AuthManager, device keypair, tokens, Tauri commands) & `plethora_cloud` (CloudJobService); Frontend `useAccountStore`, `UserProfilePanel` real account + connected devices registry, `LoginModal` account sheet, client SDK `src/lib/plethoraCloud/client.ts`; unit test suites for token rotation, reuse detection, device revocation, job lifecycle, and local data preservation. Gates on main: test:run 3536✓, cargo 850✓, bench OK (21 benchmarks PASS, 0 regressions), build OK.
- B4 — Subscription Billing, Zero-Knowledge E2E Encrypted Cloud Sync & Intelligence Contracts (Proposal 4 + 6 + 7-m1) merged on main. Deep review completed for money/crypto/memory invariants. Proposal 4: Server billing endpoints (`/v1/billing/validate`, `/v1/billing/subscriptions`, `/v1/billing/restore`, `/v1/billing/webhooks/:provider` with idempotency deduplication and `purchases`/`subscription_events`/`webhook_dedupe` tables), `BillingProvider` interface, `MockBillingProvider`, and `useBillingStore`. Proposal 6: Zero-knowledge record delta sync architecture with AES-256-GCM authenticated encryption + AAD binding, 32-byte BIP39 `RecoveryKey` derivation, bounded outbox/inbox queues, strict 500-record/8MB cursor pull pagination, server sync tables & endpoints (`/v1/sync/push`, `/v1/sync/pull`, `/v1/sync/data`), Rust `SyncEngine` (`src-tauri/src/sync/`), `useSyncStore`, and `SyncSettingsPanel`. Proposal 7 Milestone 1: Core RAG contracts (`RagHit`, `RagCitation`, `RagLocator`, `SourceKind`), format locator normalization helpers, and `filterCloudEmbeddingCandidates` ensuring `isLocalOnly` documents never leave the device. Gates on main: test:run 3546✓, cargo 854✓, bench OK (21 benchmarks PASS, 0 regressions), build OK, scripts 109✓.
- B5 — Intelligence Core, Semantic Connections, Personal Knowledge Graph & Intelligent Flashcards (Proposal 7-rest + 8 + 9 + 13) merged on main. Proposal 7-rest: Whole-library RAG & structured citation layer with `CitationChips` and deep-link locator routing across PDF, EPUB, and video formats. Proposal 8: Automatic semantic connections engine with calm reader margin affordance (`ConnectionsAffordance`), structured relation suggestions (`same_as`, `prerequisite_of`, `contradicts`, `supports`, etc.), dismissal fingerprint deduplication, and `useConnectionsStore`. Proposal 9: Personal Knowledge Graph substrate (`ConceptNode`, `ConceptEdge`), evidence provenance, neighborhood graph traversal, concept merging, and `useKnowledgeGraphStore`. Proposal 13: Intelligent multi-form flashcard generation pipeline (`qa`, `cloze`, `conceptual`, `application`, `compare_contrast`), Wozniak formulation heuristic scoring, and token Jaccard duplicate prevention. Gates on main: test:run 3557✓, cargo 854✓, bench OK (21 benchmarks PASS, 0 regressions), build OK, scripts 109✓.
- B6 — Cloud Media & Ingestion Capabilities: Document Reconstruction, Neural TTS Audiobooks, Transcription Diarization & Pro Public API (Proposal 16 + 17 + 18 + 20) merged on main. Proposal 16: `document_reconstruct` pipeline types, canonical reflow integration, and cloud-eligibility filters enforcing local-only and AI-exclusion shields. Proposal 17: `plethora` neural cloud voice adapter (`plethoraAdapter.ts`), listening queue store (`useListeningQueueStore`), and timestamped audio-reading sync. Proposal 18: `transcribe` job diarization models (`DiarizedSegment`), structured document generator from speaker turns with timestamp locators, and YouTube cloud-boundary compliance. Proposal 20: Pro Public API REST endpoints (`/v1/api/*`), scoped API token management (`api_tokens`, `useApiTokensStore`), and HMAC-SHA256 signed event webhooks (`webhooks`). Gates on main: test:run 3565✓, cargo 854✓, bench OK (21 benchmarks PASS, 0 regressions), build OK, scripts 109✓, server 10✓.
- B7 — Remote Capture & Adaptive Learning Intelligence: Web Inbox, Knowledge Gaps, Socratic Tutoring & Card Optimizer (Proposal 19 + 10 + 12 + 14) merged on main. Proposal 19: Remote web capture endpoints (`/v1/capture/url`, `/v1/capture/content`, `/v1/inbox`), SSRF guards, and client [`useInboxStore`](file:///Volumes/external/mac-mini/Code/Plethora/src/stores/inboxStore.ts). Proposal 10: Knowledge Gap detection models (6 gap kinds), Bayesian mastery estimation with variance, and [`useKnowledgeGapsStore`](file:///Volumes/external/mac-mini/Code/Plethora/src/stores/knowledgeGapsStore.ts). Proposal 12: "Teach Me" adaptive Socratic lesson state machine ([`lesson.ts`](file:///Volumes/external/mac-mini/Code/Plethora/src/lib/ai/tutor/lesson.ts)) with diagnostic calibration, misconception remediation, and weakness-derived flashcards. Proposal 14: Card lifecycle optimization proposals (rewrite, split, merge, retire, generate application), non-destructive preview models, and [`useCardOptimizerStore`](file:///Volumes/external/mac-mini/Code/Plethora/src/stores/cardOptimizerStore.ts). Gates on main: test:run 3574✓, cargo 854✓, bench OK (21 benchmarks PASS, 0 regressions), build OK, scripts 109✓, server 12✓.





