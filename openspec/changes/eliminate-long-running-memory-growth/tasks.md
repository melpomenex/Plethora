> Workstream tags: **[A]** macOS measurement · **[B]** Blob/audio/TTS lifetime · **[C]** instrumentation + diagnostics · **[D]** soak + gate. Dependencies between sections are stated in the section headers; sections without a stated dependency are independently implementable (see `design.md` D12 for file ownership).

## 1. Phase 0 — Incident capture and current-state audit

- [x] 1.1 [A] Write the macOS incident section of `docs/memory-profile.md`: the 74 GB observation (duration, OS, hardware, build if known), the verified code findings from this change's `proposal.md` with file:line references, and the regression window (all verified mechanisms introduced 2026-08-19 → 2026-08-23; `git log -S` evidence for each). Mark each finding **verified code hazard, magnitude unattributed**.
- [x] 1.2 [A] Add the one-command capture script `scripts/memory-bench/capture-tree.sh` (diagnostic artifact, not a collector): `ps -axo pid,ppid,%cpu,rss,vsz,etime,command` filtered to the Plethora tree + per-pid `vmmap -summary`, `leaks`, `sample <pid> 10` output, timestamped into a directory. Document in `docs/memory-profile.md` as the first thing to run if the incident recurs before the harness lands.
- [x] 1.3 [C] Confirm at HEAD, with tests or code citation recorded in the incident section, that no *other* unbounded global accumulator was added by the 2026-08-19 → 2026-08-23 reliability/feature batch: grep-audit new module-level `Map`/`Set`/array caches and `window.*` assignments introduced in that window; list each with bounded/unbounded verdict.
- [x] 1.4 [A] Correct the stale `INCREMENTUM_MEMORY_SCENARIO`/`INCREMENTUM_MEMORY_CONTROL` references in comments at `src-tauri/src/commands/memory_scenario.rs:10-11,40` and `src/lib/memoryScenario/config.ts:5` to the actual `PLETHORA_MEMORY_*` constants (constants already match; comments only).

## 2. Phase 1 — macOS measurement infrastructure [A]

Depends on nothing. Section 8 (WS-D driver edits) depends on this section landing first.

- [x] 2.1 [A] Create the native helper crate `scripts/memory-bench/native/macos-footprint/` (cargo, `libproc`/`proc_pid_rusage` bindings only): given a root PID, enumerate the process tree (PPID walk via `proc_pidinfo`), and for each PID output JSON `{ pid, ppid, executable, roleHint, physFootprint, residentSize, wiredSize, virtualSize }` using `RUSAGE_INFO_CURRENT` / `ri_phys_footprint`. A PID that exits mid-walk is reported absent, never an error.
- [x] 2.2 [A] Unit-test the helper's classification against fixture inputs (executable-name → role mapping: app binary → `native`, `com.apple.WebKit.WebContent*` → `web-content`, `com.apple.WebKit.Networking` → `network`, GPU/other → `other`) and its absent-PID behavior, with the syscall layer mocked.
- [x] 2.3 [A] Implement the driver-side collector module `scripts/memory-bench/macos-footprint.js`: invoke the helper, verify tree membership by ancestry from the launched PID, apply the run-ID marker check where the helper can read a process's environment and record `markerVerified` per process (false + ancestry-verified is acceptable; documented per D3), and normalize each process into the existing per-process sample shape (footprint fields alongside the Linux `Pss`-family keys; `Rss` populated, never summed).
- [x] 2.4 [A] Extend `checkMemoryCollectionSupported` (`platform.js`) to accept `darwin` when the helper binary is present and executable, keeping the Linux path and all refusal reasons unchanged; non-Linux-non-macOS still refuses.
- [x] 2.5 [A] Make the tree total and settle logic platform-aware: on macOS the tree total is the sum of per-process physical footprints (never RSS) and the settle convergence check reads the footprint total. Unit-test both with fixture samples.
- [x] 2.6 [A] Add macOS branches to `collectEnvironment` (`result.js`): OS version (`sw_vers`), kernel (`uname -r`), CPU model (`sysctl -n machdep.cpu.brand_string`), total RAM (`sysctl -n hw.memsize`), WebKit engine reported as the OS version, display server `null`. Extend the machine-profile fields consumed by the comparator with a `platformKind: "linux" | "darwin"` discriminator and test that a Linux baseline is refused against a Darwin result (exit 2, fields named).
- [ ] 2.7 [A] Build the helper in `bench:memory` when on darwin (npm script or driver-driven `cargo build`), wire `driver.js` platform dispatch (collector selection only — no scenario changes), and end-to-end verify one full existing reader scenario on macOS producing a result file with per-process roles.
- [ ] 2.8 [A] Record the first macOS "before" numbers in `docs/memory-profile.md` (Phase 2 of the doc): fresh idle / one doc / four tabs / post-close / cycles, per role. If the numbers already show the incident class at bench scale, capture tree samples per stage as the first attribution artifact.

## 3. Phase 2 — Resource-lifetime diagnostics [C] (creates primitives WS-B depends on)

Depends on nothing. Sections 5 (WS-B adoption) depends on 3.1.

- [x] 3.1 [C] Create `src/diagnostics/ownedObjectUrl.ts`: `createOwnedObjectUrl(blob, { owner, ownerId })` / `revokeOwnedObjectUrl(url)` / `revokeAllOwnedObjectUrls(owner, ownerId?)` over a registry that stores `{ url, owner, ownerId, bytes, created }` only — never the Blob — and reports `getOwnedObjectUrlStats()` → totals and per-owner `{ count, bytes }`. Dev/test-only bounded allocation-site ring behind the diagnostics gate. Unit tests: create/revoke accounting, replace-revoke helper, byte totals, teardown-to-zero assertion, and a test asserting the registry holds no reference to the Blob (weakref/GC-flavored test with the diagnostics gate on).
- [x] 3.2 [C] Create `src/diagnostics/errorRecorder.ts` implementing D6: signature aggregation `{ signature, type, message(truncated), sampleStack?, count, firstSeen, lastSeen }`, cap on distinct signatures (default 64, oldest-count evicted), inert in production unless the diagnostics gate is on. Install it in `src/main.tsx` replacing both `__plethoraTestErrors` push sites (`main.tsx:91-93`, `main.tsx:115-117`). Tests: 10,000 identical errors → one aggregate with `count: 10000` and bounded retained bytes; >cap distinct signatures evicts; disabled-by-default in a production-config test.
- [x] 3.3 [C] Migrate the iOS reliability harness consumer from the `__plethoraTestErrors` array to `getDiagnosticSnapshot().errors` (aggregate form) in the same PR; update `openspec/changes/establish-ios-reliability-harness-and-crash-observability/tasks.md` only if it documents the array's shape (compat adapter only if the harness cannot change).
- [x] 3.4 [C] Gate the permanent heartbeat (`src/main.tsx:580-583`) behind the same diagnostics/harness env gate; assert in a production-config test that no `data-plethora-heartbeat` interval is installed when the gate is off, and that the reliability harness path still gets its liveness attribute when on.
- [x] 3.5 [C] Create `src/diagnostics/resourceCounts.ts` + a single `getDiagnosticSnapshot()` composing: owned-URL stats (3.1), error aggregates (3.2), `sectionAudioBlobCache` size, TTS in-flight count, audio-edition job count, TTS cache entry count/total bytes (via `getCacheSize`), and IndexedDB connection count (exported counter from 5.3). Expose only through the diagnostics gate (structured object; no production global). Unit-test that the snapshot is JSON-serializable, content-free (no user text/audio), and cheap (< 1ms for 10⁴ tracked URLs in bench terms).
- [x] 3.6 [C] Extend the in-app scenario host (`src/lib/memoryScenario/`) with a `diagnostics` report op returning `getDiagnosticSnapshot()` to the driver, gated by the existing scenario env gate; test it is absent under production configuration.

## 4. Phase 3 — Deterministic reproduction scenarios [D]

Depends on 2.x (driver runs on macOS) for macOS runs; scenario code itself is platform-neutral and can be built against Linux immediately.

- [x] 4.1 [D] Add scenario stages `tts-cycles` (N × synthesize → play → stop → dispose, cache-hit and cache-miss variants via alternating texts) and `edition-cycles` (generate K sections → play → cancel mid-run → delete edition → retry → cleanup), driving the same store actions the UI uses, in `scenario.js` + the in-app host ops. Default N/K documented; corpus gains a long-text fixture for deterministic synthesis volume (mock/synthetic provider in scenario mode — no network, no paid providers).
- [x] 4.2 [D] Add the idle-soak stage: `idle-soak` holds the app idle for a configured duration while the driver samples the tree **periodically** (default 30s) rather than only at settle; tier presets `quick` (5–10 min), `dev` (30–60 min), `nightly` (4h+), `extended` (10–12h+) as named configs in `scenario.js`, wired to `npm run bench:memory -- --soak=<tier>`; every soak sample keyed by elapsed time in the result file.
- [x] 4.3 [D] Add the synthetic-leak injection to the scenario host: `PLETHORA_MEMORY_SYNTHETIC_LEAK_MB_PER_CYCLE=<n>` retains n MB per cycle step in a module sink. Assert in a production-config test that the sink is absent without the scenario env gate.
- [ ] 4.4 [D] Run each new stage on Linux immediately and on macOS after 2.7; archive stage results in `docs/memory-profile.md` as reproduction evidence: which stage, if any, reproduces unbounded growth without user interaction.
- [x] 4.5 [D] Document the tiers, defaults, and the "Mac mini runs `nightly`/`extended` unattended" operating procedure in `docs/memory-profile.md` and the harness README.

## 5. Phase 4-fix — Blob/audio/TTS lifetime bounds [B] (3.1 required; evidence from 4.4 tunes caps)

- [x] 5.1 [B] Route TTS provider result URLs (`src/api/tts/providers/shared.ts:85`, `openrouter.ts:102`) and persistent-cache-hit URLs (`src/api/tts.ts:211,217`) through `createOwnedObjectUrl` with owner `tts-synthesis` / `tts-cache-hit` (+ ownerId = cache key or section id); keep the `audioData` return contract unchanged for callers that need it.
- [x] 5.2 [B] Bound `sectionAudioBlobCache` (`audioEditionGenerationStore.ts:58`): LRU with documented count cap and byte cap; `revokeOwnedObjectUrl` on evict and on replace (fixes the retry-orphan at `audioEditionGenerationStore.ts:232`); entries owned as `edition-section` + section id; explicit `revokeAllOwnedObjectUrls` on edition deletion (hook `deleteAudioEdition`), job cancellation (`cancelJob`), and library reset. Tests: eviction order, revoke-on-replace, revoke-on-cancel, revoke-on-delete, byte/count bounds enforced, and post-delete live URLs for that edition are zero.
- [x] 5.3 [B] Convert `src/utils/ttsCache.ts` to one managed shared connection: module-level promise-cached `IDBDatabase`, reused by all operations, closed on `clearAudioCache` and page teardown where observable; export the connection counter for 3.5. Test: 100 sequential `getCachedAudio` calls open exactly one database (spied `indexedDB.open`) and close count is 0 until clear; repeated lookups never accumulate handles.
- [x] 5.4 [B] Implement metadata-only eviction (D8): per-entry metadata `{ key, size, lastAccessed }` maintained in a dedicated store (or key-only cursor path), eviction computes victims and deletes by key without reading any `audioData` payload; one-time additive backfill for pre-existing entries (missing metadata ⇒ evictable last, size counted on first access); preserve the 500 MB default, mutex, and QuotaExceeded recovery. Tests: eviction under bound never calls the payload-store `get`/cursor-value path (spy), cache converges under its configured size, and existing entries from the old schema remain readable.
- [x] 5.5 [B] Replace whole-file desktop playback (`AudiobookViewer.tsx:1686-1693` and the podcast-download fallback sibling) with `get_media_stream_url` streaming as the desktop default (the command already exists for mobile); keep an explicitly bounded blob fallback (documented MB cap with size check before materialization) for stream-unavailable cases; revoke per 3.1/D5 on every source swap. Tests: desktop path requests a stream URL and never calls `readDocumentFile` for over-cap files; fallback respects the cap and revokes the prior URL.
- [x] 5.6 [B] Convert `AudiobookViewer`'s full-edition playlist (`AudiobookViewer.tsx:939-960`) to the playback working set (D7): current + next 1–2 sections resolved from the bounded cache with prefetch across section boundaries; nothing revoked under active playback; playlist metadata (durations, chapters) computed from section records, not from live URLs. Tests: advancing through K sections keeps live URLs ≤ working-set size; boundary prefetch prevents a gap in scenario mode.
- [x] 5.7 [B] Adopt owned URLs at `CreateAudioEditionDialog.tsx:155` (preview) with revoke-on-dismiss; audit remaining `createObjectURL` sites in audio/media paths (`localMediaSource.ts`, `MediaLibrary.tsx`, `RSSReader.tsx`, `VideoPlayer.tsx`) and convert the ones with unbounded lifetimes, recording each conversion in the incident section (site → owner → bounded-by-what).

## 6. Phase 4 — Deep profiling and attribution [A] (requires 2.7; runs against pre-fix and post-fix builds)

- [ ] 6.1 [A] Document and execute the manual workflow (D10) in `docs/memory-profile.md`: Instruments Allocations with generations + Leaks + VM Tracker over checkpoints A–F on macOS; WKWebView JS heap snapshots at A/B/C/D/E comparing `ArrayBuffer`, `Uint8Array`, `Blob`, strings, detached DOM, `HTMLCanvasElement`, `ImageData`, pdf.js objects, media elements, listener closures, reader/store objects; retaining paths for everything unexpected alive at C.
- [ ] 6.2 [A] From the profiling data plus harness stages, classify the macOS growth per D10's buckets (live-reachable / allocator high-water / WebKit backing stores / reclaimable / true native leak) and attribute the 74 GB class to specific mechanisms, writing the classification with evidence. Fixes beyond section 5 must cite these findings; drop or re-scope anything the evidence does not support.
- [ ] 6.3 [A] If attribution is inconclusive after 6.2 (application resources flat, footprint still climbs), build the D14 control app (one Tauri window, pinned `tauri = 2.11.0` / wry 0.55.1, same WKWebView init, no Plethora features) under `scripts/memory-bench/control-app/`, soak it comparably, and record the interpretation matrix result. No dependency upgrades result from this change.

## 7. Phase 6 — Repeated-use verification [B/D]

Depends on 4.x and 5.x.

- [ ] 7.1 [B] Using the scenario `diagnostics` op (3.6), verify after N edition cycles and N TTS cycles: live owned URLs per owner return to pre-cycle baselines, `sectionAudioBlobCache` within caps, TTS connection count stable at 1, error-aggregate memory bounded under a forced repeating error. Doubling N must not approximately double any of them. Record in `docs/memory-profile.md`.
- [ ] 7.2 [D] Run reader + TTS + edition cycles at N and 2N on macOS; confirm per-role footprint convergence (post-warmup slope within gate allowance; final-vs-reference within allowance) and that the residency cap actually bounds simultaneous heavy readers (1/2/4-reader stage matrix with `readerTabCap` honored — observed, not assumed).
- [ ] 7.3 [D] Record post-fix per-stage macOS numbers in `docs/memory-profile.md` side-by-side with 2.8's "before" numbers and the accepted high-water behavior (what legitimately does not return to baseline and why).

## 8. Phase 7/8 — Soak, baseline, and gate [D] (requires 4.2, 6.2; driver edits after 2.x)

- [ ] 8.1 [D] Run the `nightly` soak on the Mac mini against the fixed build; save per-sample per-role results with the D10 artifact set on failure; a failed soak must leave enough to name the process, role, resource category, growth shape, and start time.
- [x] 8.2 [D] Extend `check-memory-budget.mjs` with soak and role metrics: `idle-growth-per-hour` (post-warmup OLS over soak samples), `soak-final-vs-reference`, and per-role (`native`, `web-content`) variants of the existing cycle ratchet metrics — same clamped-threshold and exit-code conventions; unit-test slope math on fixture soak series.
- [ ] 8.3 [D] Gate self-test: run the harness with `PLETHORA_MEMORY_SYNTHETIC_LEAK_MB_PER_CYCLE` set to a value that stays under every peak ceiling; `bench:memory:check` must FAIL on the ratchet/slope while all peak metrics pass. Also verify a stable high-water fixture and a noisy-but-bounded fixture pass within configured variance.
- [ ] 8.4 [D] Record macOS baselines in `scripts/memory-baselines.json` (per D6-of-predecessor conventions: per-metric baseline, allowances, variance, sample count, gated/diagnostic class, ratchet parameters, machine profile with `platformKind: "darwin"`); sanity-check: synthetic doubling of a gated macOS metric fails; repeated unchanged runs on the same machine pass.
- [ ] 8.5 [D] Optional and non-blocking: nightly GitHub Actions macOS soak job (`macos-14`, `nightly` tier, `--warn-only`, artifacts uploaded); no required checks added. Decide runner-cost trade-off and record the decision.
- [ ] 8.6 [D] Update `docs/memory-profile.md` to final form for this change: architecture/lifetime model (URL ownership, cache policies, connection lifecycle), reproduction, before numbers (2.8), attribution (6.2), fixes with deltas (7.1–7.3), after numbers, accepted high-water behavior, baseline machine profile, remaining limitations.

## 9. Verification

- [ ] 9.1 Run `npm run test:run`, `npm run test:scripts`, `npm run lint`, `npm run bench:check`, and the Rust test suite; all pass. Manually verify on macOS: audiobook section playback across boundaries, edition generate/cancel/delete/retry, reader TTS with cache hits, PDF/EPUB open/close cycles, and the reliability harness's liveness probe with the diagnostics gate on.
- [x] 9.2 Confirm the production build contains no diagnostics surface: no heartbeat interval, no error aggregates retained, no owned-URL registry writes outside gated owners, no scenario host, no synthetic-leak sink (production-config tests + one manual release-build check).
