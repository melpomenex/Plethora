## 1. Persistent cache hardening

- [x] 1.1 Audit `src/utils/ttsCache.ts` + `src/api/tts.ts:generateSpeech`/`cacheAudioResult` and document the current missing key dimensions vs `TTSAdapterCapabilities` (instructions/preset/language/pronunciationDictionary/cloned-voice/baseUrl) in a `design.md` decision table; implement `digestJson128` helper and fix `setCachedAudio`'s `meta.totalSize` double-count on overwrite (check existing key size first).
- [x] 1.2 Extend `TTSCacheEntry` with `wordTimings?: WordTiming[]` and `source?: string` metadata; bump `DB_VERSION` to 2 if needed with an `onupgradeneeded` that preserves v1 rows (no bulk clear); add corrupt-entry validation (`audioData instanceof ArrayBuffer`) that deletes and returns `null` on `DataError`.
- [x] 1.3 Implement `makeTTSCacheKeyV2(params)` (canonical `provider,model,voice,speedNormalized,format,languageDigest?,instructionsDigest?,presetDigest?,pronunciationDigest?,clonedVoiceDigest?,baseUrl? ,textDigest`) with `speed` normalization (`toFixed(3)` strip), `foldForMatch`-based canonicalization for text-adjacent fields, and helper table for per-provider inclusion rules; keep `makeCacheKey` as a shim emitting the legacy shape.
- [x] 1.4 Switch `generateSpeech` read path to `V2` then legacy alias lookup; switch write path to `V2`; add one-shot lazy alias adoption (on legacy hit, re-key under `V2` if not present) behind a feature flag/dev guard if risk is non-trivial.
- [x] 1.5 Harden `ensureCacheSize`: reuse `cacheMutex` promise chain to serialize concurrent evictions, open a fresh `readwrite` transaction for deletes (not the read-cursor txn), handle `QuotaExceededError`/`VersionError`/`AbortError` explicitly, and never clear the persistent store on `bufferMgr.evictPlayedChunks` (assert separation with a test).
- [x] 1.6 Define the **durable write gate** for paid providers (`fal,elevenlabs,openai,openai-compatible,openrouter,plethora,groq`): new `setCachedAudioDurable(key, buffer, durationSec, wordTimings?) → Promise<void>` that `await`s commit, evicts on `QuotaExceededError` then retries once, and rejects only on persistent IDB failure; `generateSpeech` `await`s it when `awaitCache=true` (true for paid tiers, false/void for `system/android/pocket`). Wire a caller-level opt-in so `ReaderTTSControls` can play immediately while awaiting commit in the background without marking the entry `ready` in cache diagnostics until committed.
- [x] 1.7 In-flight deduplication: create `src/api/tts/dedup.ts` (or in-module map) `Map<string, Promise<GenerateSpeechResult>>` with `getOrCreateTTSGeneration(key, factory)` + `finally` cleanup; integrate at top of `generateSpeech`'s miss path and in `ReaderTTSControls.preBufferChunks` (skip enqueue when `deduper.has(key)`); add `signal?: AbortSignal` plumbing so epoch bumps abort the awaiting promise linkage (underlying `fetch`/IDB write allowed to settle without playing).
- [x] 1.8 Thread measured `wordTimings` through `cacheAudioResult`/`setCachedAudioDurable` into the cache entry and back out on `getCachedAudio`; ensure `BufferedAudio.wordTimings` populated from cache on hit (so `startWordTracking` uses measured data without re-synthesis); never cache `synthesizeWordTimings` approximate results as `measured`.
- [x] 1.9 Diagnostics (dev-only): add `cacheSource: 'memory'|'persistent'|'synthesized'|'system'|'android-native'` annotation in `BufferedAudio` and `console.debug("[TTS cache]", {key, legacyKey, source, hasWordTimings, durationSec})` at `debug`/DEV guard; do not log text or credentials. Verify normal UI shows no cache badges — diagnostics are log-only unless `settings.debug.ttsDiagnostics` enabled.
- [x] 1.10 Cache hit-visibility test: synthesize segment A, persist, remount `ReaderTTSControls` (new index + new deduper instance), request A again → assert provider spy `calledOnce`, second `durationSec`/`wordTimings` come from cache, `QuotaExceededError` recovery leaves cache bounded.
- [x] 1.11 Cache key-correctness tests: same text + different `voice` → miss; different `model` → miss; different `speed` → miss; different `instructions` (when adapter `supportsInstructions`) → miss; different `preset/prompt/temperature` → miss; different `pronunciationDictionary` → miss; different `language` (fal `Auto` vs `English`) → miss; same text + same params after remount → persistent hit; `proxyUrl`/`requestMode` change → still hit (not in key). Language/instructions omitted correctly when adapter capability false.
- [x] 1.12 Concurrent-request test: two simultaneous `generateSpeech({text:"same"})` with identical V2 key → exactly one underlying `adapter.synthesize` invocation, both callers resolve to same `audioUrl`/`audioData`, deduper entry cleaned after settle, aborted epoch does not poison the deduper.

## 2. Listening position — storage & persistence layer

- [x] 2.1 Design the Rust + TS persistence shape: new `tts_listening_positions` table `{id, profile_id, document_id, updated_at, text_fingerprint, speech_fingerprint, provider, model, voice_id, stable_anchor_json, chunk_index, chunk_text_hash, word_index, normalized_char_offset, intra_chunk_ms, surrounding_text, scroll_percent_hint, cfi, page_number}` with `UNIQUE(profile_id, document_id)` and `profile_id` derived from `plethora_user.id` (`anon` fallback) mirroring `getPreferredViewStateKey`; add Tauri commands `get_tts_listening_position`, `save_tts_listening_position`, `clear_tts_listening_position` in `src-tauri/src/commands/position.rs` (or new `tts_listening_position.rs`) with browser-mode IndexedDB fallback in `src/utils/ttsListeningPosition.ts` (store `plethora-tts-positions`, same schema).
- [x] 2.2 Implement `src/utils/ttsListeningPosition.ts`: `getTTSListeningPosition`, `saveTTSListeningPosition` (with in-memory throttling — at most once per 4 s except `flush:true`), `clearTTSListeningPosition`, helpers `fingerprintDocument(text)` / `fingerprintSpeechIndex(index)` using `digestText128`/`foldForMatch` + stable section keys; handle localStorage profile-namespace drift (read `plethora_user` at call time, not module init) and corrupt JSON → delete and return null.
- [x] 2.3 Wire `DocumentViewer` flush hooks: reuse `flushAllViewStateWrites` pattern to also flush pending listening checkpoints on `tabsStore` eviction, on `DocumentViewer` unmount, and on `visibilitychange`/`pagehide`/`beforeunload` (best-effort sync write via `navigator.sendBeacon`-style or immediate `save({flush:true})`).
- [x] 2.4 Unit tests for storage: round-trip per doc, profile isolation (`anon` vs `u:id`), corrupted entry recovery, fingerprint invalidation when content changes, no-listening-position fallback returns null.

## 3. Listening position — resume algorithm & UX

- [x] 3.1 Implement the stable-anchor resolution algorithm in `src/utils/ttsListeningPosition.ts` (or `src/utils/readerSpeechIndex.ts:resolveListeningPosition`): try `locate(stableAnchor)` with `chunkTextHash` verification → `normalizedCharOffset` within owning section → fuzzy `surroundingText` `foldForMatch` search constrained to section → `cfi`/`pageNumber` fallback → visual position; return `{chunkIndex, wordIndex, method, intraChunkMs?}` or null (with diagnostics).
- [x] 3.2 Extend `ReaderTTSControls.resolveStartPosition` priority chain to include the listening checkpoint between authoritative position and saved scroll position `(explicit → viewport → authoritative → ttsCheckpoint → savedPosition → start)`; add a `resolveListeningCheckpoint?: () => SpeechPosition | null` ref (fed by `DocumentViewer`'s call to `getTTSListeningPosition` + resolution) without breaking text-only `QueueScrollPage` callers (they pass no resolver → unchanged).
- [x] 3.3 Build the lightweight non-blocking resume banner in `DocumentViewer` (or inside `ReaderTTSControls` when `hasListeningCheckpoint`): shown only when checkpoint is meaningful (`>30s` or `>2%` remaining, not at `{0,0}`, not at current viewport, not within 30 days expiry ignored), copy "Resume listening from 37:42?" with `Resume` / `Start from here` / `Start over` / dismiss, never auto-plays, auto-hides after 12 s or on first Play; wire `Resume → ReaderTTSHandle.startFrom(sourceAnchor)` at `wordIndex` (via `sliceChunkAtWord` path already correct) with `intraChunkMs`→word re-resolution when measured timings cached.
- [x] 3.4 Wire periodic + edge-triggered persistence inside `ReaderTTSControls`: throttled checkpoint write every ~4 s in `startWordTracking` + immediate writes on `onChunkChange`/`handlePrev|Next`/`startFrom`/`handleStop`/`pause`/`seek`; ensure `autoAdvance` chunk completion advances the checkpoint rather than resetting; ensure provider/voice change keeps position (only audio identity changes, anchor reused).
- [x] 3.5 Enforce visual↔listening separation: `followSpokenWord && isPlaying` coarse `ViewState`/`DocumentPosition` updates only at chunk boundaries; manual `ViewState` writes never clobber the listening table; document the rule in `design.md` and assert with a store-spy test.
- [x] 3.6 Time-display helpers: derive `37:42` from summed cached `durationSec` when available, else `chunkIndex/total` estimated plus `intraChunkMs`; fall back to "Section 3 · word 47" when duration unknown — never show NaN.
- [x] 3.7 Resume tests (per design): pause mid-chunk + close + reopen → resumes at same sentence/word; app restart where SQLite/IDB persists → resumes; PDF/EPUB/Markdown/HTML each resume correctly; after `CHUNK_MAX` config change (re-chunking) → stable anchor still resolves; after document text edit → stale anchor recovered or gracefully falls back; stale checkpoint (content hash mismatch beyond threshold) → fallback to closest position not random duplicate; after provider/voice change → position valid, next synthesis cache miss; invalid/corrupt checkpoint → fallback without crash; resume at intra-word time uses cached measured timings when present else approximate.

## 4. Desktop discoverability & setup sheet

- [x] 4.1 Redesign `DocumentViewer` reader chrome to host a permanent **Listen** button (`SpeakerHigh` + "Listen", `aria-label="Listen to this document"`) for supported types (PDF/EPUB/Markdown/HTML article) irrespective of `tts.enabled`; move the existing `ReaderTTSControls` playback bar out of `absolute z-40` into the normal footer/chrome flow (flex row with `QueueNavigationControls`/`DocumentMinimap`/rating chips) so it never overlaps the document or Queue controls.
- [x] 4.2 Change `ReaderTTSControls` gating: keep imperative handle + `playbackState()` available when `!ttsEnabled`; split the component into (a) always-available affordance button (rendered by `DocumentViewer`) and (b) playback bar gated on `tts.enabled && validateTTSConfiguration(settings.tts).valid`; `DocumentViewer` now owns the Listen button that either opens the bar (configured) or opens the setup sheet (unconfigured).
- [x] 4.3 Build the lightweight setup sheet (`src/components/viewer/TTSSetupSheet.tsx` or `src/components/tts/ListenSetupSheet.tsx`) — a Dialog/BottomSheet (reuse existing Plethora dialog primitives) listing: header "Listen — Choose how Plethora should read this document", row "Use System Voice" (when `systemTtsAvailable` from `useSystemVoices`) → one-tap `updateSettings({tts:{...enabled:true, provider:"system"}})` + immediate `ReaderTTSHandle.startFrom(viewportAnchor)`, dynamic cloud rows from `listAdapters()` filtered (`pocket` hidden off-Tauri, `android` off-non-mobile) annotated "Needs API key" dimmed, and "Configure more…" linking to `Settings → Text to Speech`. Validate that `tts.enabled` flips to true on System selection and that existing disabled users see the button on next open.
- [x] 4.4 Redefine `tts.enabled` semantics in `src/utils/ttsSettings.ts` docs/comments and `validateTTSConfiguration` consumers: `enabled===false` means "not yet configured; discoverable, not auto-buffering" — not "hide feature". Guard `preBufferChunks` effects (already `if(chunks.length>0 && ttsEnabled)`) to stay off when disabled; ensure no regressions to `canReadAloud` — update the gate to `supportedType && (ttsHandleRefAvailable && (documentHasRenderableText || systemTtsAvailable))` so selection "Read from here" remains available even if `!ttsEnabled` when System is usable.
- [x] 4.5 i18n: add keys for the Listen button, setup sheet header/rows/help, resume banner, and cache section across locales (or at minimum `en` with fallback placeholders).
- [x] 4.6 Discoverability component tests: supported doc + `enabled=false` → Listen button visible and sheet opens on click; `systemTtsAvailable` → "Use System Voice" succeeds and playback starts; cloud row "Needs API key" navigates to settings (no playback); `audio`/`video`/`youtube` doc types → no Listen button; chip never hidden behind  `return null` from `ReaderTTSControls`; keyboard/aria checks pass; selection "Read from here" not gated solely on `tts.enabled` when System path exists.

## 5. Spoken-word highlighting — harden default-on, timing, and mapping

- [x] 5.1 Migration hardening: keep `TTS_SETTINGS_SCHEMA_VERSION=4`; change only migration semantics so pre-v4 persisted users whose `schemaVersion<4` and `highlightSpokenWord===undefined` migrate to `true` (not false) — update `migrateTTSSettings` comment and add a regression test that the old default-off spec is superseded; `sanitizeTTSSettings` fallback stays `true` for any non-boolean.
- [x] 5.2 Cache timing reuse: ensure `BufferedAudio.wordTimings` on cache hit carries `source:"measured"` entries from `TTSCacheEntry.wordTimings` and that `startWordTracking(audio, chunkText, buffered.wordTimings)` receives them (avoiding re-synthesis when cached); verify `wordTimingsAlignWith` gate keeps mismatched cached timings from mis-highlighting.
- [x] 5.3 Timing ladder coverage: audit each paid adapter (`elevenlabs`, `fal`, `openai`, `openai-compatible`, `plethora`) for `supportsWordTimings` accuracy and `includeTimings:true` request-path normalization via `src/api/tts/timing.ts`; keep `system` `onboundary` charIndex→word via `normStart/normEnd` binary search and `android` `onWordPosition` path (sherpa sentence-anchor + synthesized fallback marked `approximate:true`) unchanged, and `groq`/`pocket`/`openrouter` correctly flagged `supportsWordTimings:false` so they always synthesize; add `digest`/`foldForMatch` mismatch tolerate tests.
- [x] 5.4 DOM mapping review (EPUB iframes, PDF fixed text layer, PDF reflow `[data-w]` spans, OCR-HTML, Markdown/HTML flattened-text offsets): verify `WordHighlighter.highlightAnchoredWord` never does an unbounded `document.querySelectorAll("iframe")` per word (allow per-section resolution only), respects `sectionContainers` authoritative routing for EPUB, validates `foldForMatch(chunk.text.slice)` before applying spans, with fallback hierarchy exact-word → best-range → synthesized-approx → chunk-level → none (never duplicate-text occurrence). Fix any remaining global `indexOf` fallbacks in `highlightWord`.
- [x] 5.5 Highlight lifecycle & style: assert `useChunkLevel` not used for anchored readers (only legacy queue-scroll caller), `timingApproximate` still toggles `--approx` softer tint vs measured strong `primary/22`, e-ink flat variant intact, `clear()` resets `previousRanges`, `destroy()` cleans styles/listeners; `isPaused` freezes `wordOffset` (already correct).
- [x] 5.6 Preferences UI: ensure **Settings → Text to Speech** shows a **Highlight words while reading aloud** toggle (already implicit via v4 `highlightSpokenWord`; add/keep the labeled row with copy "Highlights the current word as Plethora reads the document aloud.", default on, keyboard reachable, `aria-pressed` being derived from `highlightSpokenWord`).
- [x] 5.7 Tests: default-on for new installs (fresh settings → highlighted), migrated existing users → highlighted, toggle off clears highlight and persists after reopen/restart, word advances on rAF ticks against measured sample fixtures, pause keeps current word, resume continues correctly, next/prev chunk resets mapping correctly, duplicate text never highlights wrong paragraph, PDF fixed vs reflow, EPUB iframe (single + multi-iframe continuous), Markdown/HTML, measured vs boundary vs synthesized vs chunk-fallback ladder verified per provider fixture.

## 6. Settings: Downloaded speech (cache management UI)

- [x] 6.1 Build the **Downloaded speech** section in `src/components/settings/TTSSettings.tsx` (after the existing provider/voice/preset/selection): fetch `getCacheSize()` on mount and on `storage`/`visibilitychange` and after mutations; display `"{miB} · {count} segments of {maxMiB}"` with `bytesToMiB` rounding, status polling at most once per 10 s (not per render), copy "Plethora stores generated speech on this device so replaying the same passages does not require another TTS request."
- [x] 6.2 Controls: `Manage` → `Select` for max (`250/500/1000/2000/Unlimited`) calling `updateMaxCacheSize(mb)` with confirmation that growth is immediate and shrinkage triggers LRU eviction (reuse `ensureCacheSize`); `Clear cached speech` → confirmation dialog "This removes all cached speech. Next playback will regenerate via your provider." → `clearAudioCache()`; show `entryCount` and `totalSize` updating live without page reload.
- [x] 6.3 Styles reuse Plethora `border-border bg-card rounded-xl p-5` patterns; keep any existing settings navigation/deep-link working; not alarming/technical — no byte-count dominates on first load.
- [x] 6.4 Tests: size display reflects `getCacheSize` stub, `updateMaxCacheSize` call with correct bytes, Clear with cancel vs confirm, boundary 0-entry empty state.

## 7. Performance, diagnostics, and failure-path hardening

- [x] 7.1 Throttle listening writes to the budget (≤1 persistent write per 4 s steady-state, immediate on meaningful edges); ensure `setViewState` (350 ms) and listening writes never starve the main thread (use `requestIdleCallback` / microtask where available and plain `setTimeout` elsewhere); assert `WordHighlighter` / `visibleText` no-ops when hidden and no per-frame `document.querySelectorAll("iframe")`.
- [x] 7.2 Fix the IDB write-after-read transaction separation (separate write txn for eviction), handle `QuotaExceededError` by evicting then retrying once, handle `DataError`/`AbortError` by deleting corrupt keys; serialize concurrent evictions behind a `cacheMutex`.
- [x] 7.3 Keep e-ink / reduced-motion correctness: highlight and follow already respect `useIsEink`/`usePresentation().reducedMotion` (instant positioning, flat variant); do not add flashing animation and keep `prefers-reduced-motion` honored for any new follow-resume transitions.
- [x] 7.4 Accessibility: add `aria-label` for Listen/setup/Resume, `aria-pressed` for highlight toggle, keep TTS controls keyboard-tab ordered, cache controls keyboard-reachable, resume banner reachable via Escape dismiss.
- [x] 7.5 Benches (follow `scripts/perf-baselines.json` gate): add `src/utils/ttsCache.bench.ts` (key building + digest) and `src/utils/ttsListeningPosition.bench.ts` (resolve/recovery over synthetic `ReaderSpeechIndex` sections) using `seededRandom` and module `sink` — or reuse/extend `src/utils/readerSpeechIndex.bench.ts`; run `npm run bench:check` and update baselines in the same PR if intentional costs changed; add `bench:results` artifact handling notes in design.

## 8. Validation & release

- [x] 8.1 Run `openspec validate harden-tts-caching-resume-discoverability-highlighting --strict` and fix schema/coverage failures; ensure every modified capability has a spec delta and every new one has a spec file (this tasks.md covers all).
- [x] 8.2 Full frontend pass: `npm run test:scripts` (benchmark helpers) + frontend `vitest` tts/reader/viewer scopes + e2e sanity (play → pause → close → reopen → resume) on desktop Tauri build.
- [x] 8.3 Manual pass: reopen after close does not re-request charged segments (network spy confirms), retry after provider/voice change regenerates only the new key, cache hit restores measured highlighting, System immediate playback works with word boundaries, Listen discoverable when disabled, resume banner never auto-plays.
