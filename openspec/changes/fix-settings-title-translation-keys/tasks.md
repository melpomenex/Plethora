# Tasks

- [x] Fix SyncSettings.tsx — wrap all 22 raw `t("...")` text nodes in JSX curly braces
- [x] Fix LearningSettings.tsx — wrap all 9 raw `t("...")` text nodes in JSX curly braces
- [x] Fix LLMProviderSettings.tsx — wrap all 8 raw `t("...")` text nodes in JSX curly braces
- [x] Fix OCRSettings.tsx — wrap all 15 raw `t("...")` text nodes in JSX curly braces
- [x] Fix NotificationSettings.tsx — wrap all 19 raw `t("...")` text nodes in JSX curly braces
- [x] Fix CloudStorageSettings.tsx — wrap all 4 raw `t("...")` text nodes in JSX curly braces
- [x] Verify — grep `src/components/settings/` for `>t("` to confirm zero remaining instances
- [ ] Smoke test — build and visually confirm Sync Settings page displays translated labels
