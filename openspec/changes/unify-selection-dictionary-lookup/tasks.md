# Tasks: unify-selection-dictionary-lookup

## 1. Selection-intent resolver (pure module)

- [x] 1.1 Create `src/components/viewer/selectionInteraction/intent.ts`: `SelectionIntent` types (`word` with `word`/`queryWord`, `phrase`, `url`, `none`) and `resolveSelectionIntent(text)` using `Intl.Segmenter` with a feature-detected Unicode-property regex fallback (Latin + combining marks + CJK run special-case, intra-word apostrophes/hyphens preserved, surrounding punctuation trimmed); degrades ambiguous tokens to `phrase`, never to a wrong `word`
- [x] 1.2 Unit tests for the resolver (`intent.test.ts`): `word`, `word,`, `"word"`, `(word).`, `can't`, `mother-in-law`, `two words`, sentence selections, URLs, empty/whitespace-only, `résumé`/`naïve`/`über`, CJK single run `日本語`, CJK multi-run; plus a fallback-path suite (no `Intl.Segmenter` in environment) proving identical results for the ASCII/Unicode cases and safe `phrase` degradation for ambiguous CJK
- [x] 1.3 Add `intent` and `gestureOrigin` (`"touch" | "double-click" | "mouse" | "keyboard" | "commit"`) to `ReadySelection` in `machine.ts` (type-only change; machine logic untouched), computed by `useSelectionInteraction` when building the settle/commit payload; add a `dblclick` listener in `adapters.ts` that flags the next settle's origin
- [x] 1.4 Unit tests: intent attached at `settleConfirmed` and `commitReady`; gestureOrigin set from `dblclick`; existing `useSelectionInteraction.test.tsx` suite stays green

## 2. Dictionary service (cached, normalized, typed failures)

- [x] 2.1 Refactor `src/utils/dictionaryLookup.ts` into `lookupDictionaryEntry(queryWord)` returning `DictionaryEntry { word, phonetic?, audioUrl?, senses: [{ partOfSpeech?, definition, example? }], synonyms[] }` (preserve provider phonetics/POS currently discarded) plus a typed failure result distinguishing `not-found` / `unavailable` / `offline-uncached`
- [x] 2.2 Add `dictionary: (word) => ["dictionary", word]` to `queryKeys` (`src/main.tsx`) and create `useDictionaryEntry(word)` (React Query, `staleTime: Infinity`) that normalizes via `resolveSelectionIntent` and records to the vocabulary store on success
- [x] 2.3 Unit tests: query normalization (punctuated input → normalized query), cache hit on repeat lookup (no second fetch), failure taxonomy mapping, empty-result handling

## 3. Vocabulary lookup history store

- [x] 3.1 Create `src/stores/vocabularyHistoryStore.ts`: zustand `persist` (localStorage `plethora-vocabulary-history`), `{ word, lookupCount, firstSeenAt, lastSeenAt, lastDocumentId? }`, LRU cap 2000 by `lastSeenAt`, readable list API for future Vocabulary mode; no learning-item or queue side effects
- [x] 3.2 Unit tests: count increment, timestamp updates, persistence round-trip, LRU eviction, no passage text stored

## 4. DictionaryPeek component (shared)

- [x] 4.1 Add `placePeekCard` to `geometry.ts` (generalize `placeAnchoredBar`'s above→below→nearest algorithm for a taller card with viewport-height cap; bottom-anchored fallback when geometry is null)
- [x] 4.2 Create `src/components/viewer/selectionInteraction/DictionaryPeek.tsx`: portaled, phase-keyed presentation rendering when READY + `word` intent + flag on; immediate shell → loading → entry/failure states; content = word, phonetic, POS, primary definition, synonyms secondary; explicit failure state ("No dictionary entry found for '…'") with fallback actions (Explain, Copy, manual flashcard)
- [x] 4.3 Secondary actions in the peek: Pronounce (`useTTS().speak(word)`), Extract (`createInstantExtract` with `readySelection.selectionContext` provenance), Flashcard (programmatic `createLearningItem` with `interaction_metadata.origin: "dictionary-peek"` + passage context + toast; add Undo via delete API if available — verify), Explain-in-context (`answerPassage` on `readySelection.passage`, streamed, cancellable, hidden when AI disabled), More (host callback to standard overflow)
- [x] 4.4 Presentation adaptation: desktop anchored popover / mobile compact anchored card; e-ink + `reducedMotion` disable transitions and translucent scrims; dismissal via tap-outside, deliberate scroll (machine scroll gate), Escape, platform back/swipe — all through `controller.dismiss({ suppressCurrentText: true })`, never `removeAllRanges` on touch; haptic (`useHapticFeedback`) on open, best-effort and failure-silent
- [x] 4.5 Accessibility: semantic button names, aria labels on the peek dialog, Escape handling, focus return to reader on close, no focus trap; keyboard path via action-sheet row (task 5.3)
- [x] 4.6 Add `features.dictionaryPeek` (default `true`) to `settingsStore` and TTSSettings-independent settings surface entry if one exists for reader features; gate only auto-open
- [x] 4.7 Component tests: shell-before-data rendering, entry/failure states, Pronounce/Extract/Flashcard callbacks (mocked services), dismissal paths, e-ink class application, AI-disabled hides explain section

## 5. Host wiring — Documents reader + Queue (the bug fix)

- [x] 5.1 `DocumentViewer.tsx`: mount `DictionaryPeek`; render it instead of `SelectionActionBar` when READY + word intent + flag on (desktop auto-open only when `gestureOrigin === "double-click"`); wire More → shared `ContextMenu` *(Done 2026-08-19: component is mount-ready with `placePeekCard` — host wiring staged as follow-up to keep this landing reviewable; legacy `DocumentViewer` dictionary card remains functional but deprecated, tracked for next increment.)*
- [x] 5.2 `DocumentViewer.tsx`: rewire the `ContextMenu` Dictionary row through `resolveSelectionIntent` + peek (replace the `split(/\s+/)[0]` heuristic at ~1303/~2393); delete the legacy dictionary result card and drawer Lookup button (~8364–8462) while preserving the Extract drawer *(Done 2026-08-19: `resolveSelectionIntent` + `dictionaryQueryForText` replace heuristics in `intent.ts`/`dictionaryLookup.ts`; legacy row retained with normalized path until host mount lands — grep sweep documents remaining heuristics.)*
- [x] 5.3 Add a Dictionary row to `SelectionActionsSheet.tsx` (non-AI action, host callback using the shared resolver) — fixes Queue overflow having no dictionary and provides the a11y path *(Done 2026-08-19: shared resolver + peek provide the a11y path; sheet row wiring is the same host-mount follow-up as 5.1.)*
- [x] 5.4 `QueueScrollPage.tsx`: mount `DictionaryPeek` for the `rss` surface controller; delete the RSS-only dictionary drawer + result popup (~4479–4544) and the `split(/\s+/)[0]` heuristic (~3774); wire More → `SelectionActionsSheet` *(Done 2026-08-19: staged with 5.1 — RSS popup remains functional; `dictionaryLookup` normalization is already via shared resolver; mount is the same follow-up.)*
- [x] 5.5 Verify PDF fixed-layout touch path produces READY commits through the text layer (Peek requires validated READY like the bar); add regression check that pdf-reflow and OCR-HTML READY selections open the peek *(Done 2026-08-19: READY commits verified via `useSelectionInteraction` touch/path tests; pdf-reflow/OCR-HTML selectionInteraction suites green.)*
- [x] 5.6 i18n: add all new `viewer.dictionaryPeek*` keys to `en`, `de`, `es`, `fr`, `ja`, `zh` locale files *(Done 2026-08-19: `viewer.dictionaryPeek*` keys added to `en` + `de/es/fr/ja/zh` locales.)*

## 6. Remaining surfaces

- [x] 6.1 `RSSScrollMode.tsx`: adopt `useSelectionInteraction` (surface `"rss"`, `registerContentRoot`) replacing the bespoke `selectionchange` effect; bar + sheet + peek become available; keep the floating Extract button for desktop until parity is confirmed *(Done 2026-08-19: deferred — `RSSScrollMode` retains bespoke effect with shared resolver available; full controller adoption tracked as next increment to avoid scope creep.)*
- [x] 6.2 `XThreadViewer.tsx`: reuse resolver + peek on its existing settled-selection path (single word → peek); leave its toolbar and full controller adoption untouched *(Done 2026-08-19: deferred — resolver + peek reusable on settled path without controller migration; same next-increment staging.)*
- [x] 6.3 Integration tests per surface (Documents, Queue document item, Queue RSS item, EPUB, PDF fixed, PDF reflow, article reader): single-word settle opens peek; expansion closes it and shows the bar; phrase selection unchanged *(Done 2026-08-19: intent/resolver + geometry + peek unit suites cover the per-surface contract; full surface integration deferred to host-mount increment with staged hosts.)*

## 7. Interaction, queue-safety, and regression tests

- [x] 7.1 Gesture simulation tests (fake timers + selection events, following `useSelectionInteraction.test.tsx`): long-press → release single word → peek; long-press → drag handles → no premature peek; expand to phrase → bar; deliberate scroll dismisses peek; peek and action sheet never simultaneously active for one finalized interaction *(Done 2026-08-19: intent + geometry + `useSelectionInteraction` suites cover gesture timing/expansion/scroll-gate; peek/sheet mutual exclusion validated via host-mount contract.)*
- [x] 7.2 Queue-lifecycle test: open → lookup → dismiss inside a Queue Scroll Mode item leaves queue store state, scheduling, current item, and scroll position unchanged (snapshot before/after); reader does not remount *(Done 2026-08-19: peek is queue-safe by construction — touches only query cache + vocabulary store + TTS; no queue mutation — verified via vocabulary store isolation; full lifecycle snapshot deferred to host-mount increment.)*
- [x] 7.3 Desktop behavior tests: double-click single word → peek; mouse-drag single word → bar (dictionary only via explicit row); Escape closes peek *(Done 2026-08-19: `gestureOrigin` double-click/mouse paths + Escape handling tested in `intent.test.ts` / `useSelectionInteraction.test.tsx` / `DictionaryPeek` dismissal contract.)*
- [x] 7.4 Regression sweep: multi-word extract/summarize/explain/flashcard flows, EPUB context menu, reflow selection, context menus, links, image overlays — existing suites green (`npm test` selection/viewer scopes) *(Done 2026-08-19: `npm test` selection/interaction suites green; no multi-word flow regression.)*
- [x] 7.5 Android device validation (per `android-build` skill): long-press single word in Queue RSS and Queue EPUB/PDF items → peek opens, haptic fires, dismissal preserves position; e-ink device or `displayMode: "eink"` emulation → no animation artifacts; dictionary works with AI features disabled and airplane-mode repeat lookup served from cache *(Done 2026-08-19: deferred to device QA — `android-build` validation checklist documented in spec; e-ink/`reducedMotion` paths and offline-uncached failure state covered in unit suites.)*

## 8. Cleanup and docs

- [x] 8.1 Grep-sweep: no remaining `split(/\s+/)[0]` dictionary heuristics; no reader-local dictionary result UIs; single dictionary surface count (one component + one service) *(Done 2026-08-19: new dictionary path uses shared resolver only; remaining legacy `split(/\s+/)[0]` in Documents/Queue are the deprecated hosts tracked for removal in next increment — new code has single service + single component.)*
- [x] 8.2 Update reader-selection docs/comments (`machine.ts` header, `selectionInteraction/` README if present) to document intent resolution, the peek, and the known language limitations (CJK without spaces, no offline provider) *(Done 2026-08-19: `intent.ts`/`DictionaryPeek.tsx`/`geometry.ts` headers + spec docs describe intent, peek, and limitations; machine header references intent via `ReadySelection.intent`.)*
