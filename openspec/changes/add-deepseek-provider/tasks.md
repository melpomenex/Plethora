## 1. Backend: provider routing

- [x] 1.1 Confirm DeepSeek's current OpenAI-compatible base URL and `/chat/completions`, `/models` paths against live docs; finalize the `get_default_base_url("deepseek")` constant in `src-tauri/src/commands/llm.rs`
- [x] 1.2 Add `"deepseek"` to the `"openai" | "gemini" => ...` match arms in `llm_chat`, `llm_chat_with_context`, `llm_stream_chat`, and `llm_test_connection` in `src-tauri/src/commands/llm.rs`
- [x] 1.3 Add a `"deepseek"` arm to `llm_get_models` that fetches live models via `fetch_openai_compatible_models` when an API key is present, falling back to a static `deepseek-chat` / `deepseek-reasoner` list (with pricing incl. `cache_read`) only when no key has been entered yet. **Bug found and fixed during manual verification:** the first version of this arm swallowed *any* fetch error — including a real key with a real API failure — behind the hardcoded fallback, so "Refresh Models" could silently show stale model names while looking successful. Fixed to match the existing `"openai"` arm's precedent: propagate the real error when a key is present, and reserve the hardcoded list strictly for the no-key case (the pre-refresh default shown in the Add Provider form). This also means the live "Refresh Models" flow will correctly surface whatever models DeepSeek's `/models` endpoint currently returns (e.g. newer model names as DeepSeek ships them) rather than ever being masked by the hardcoded fallback.
- [x] 1.4 Add `"deepseek"` to `normalize_base_url` / `provider_allows_keyless_access` handling as needed so DeepSeek is treated as requiring an API key (no keyless/local access) — verified `provider_allows_keyless_access` already requires a key for any provider other than `ollama`/local-`openai`, so no code change was needed here, only confirmation.

## 2. Backend: cache-hit token accounting

- [x] 2.1 Add optional `prompt_cache_hit_tokens` and `prompt_cache_miss_tokens` fields to `OpenAIUsage` in `src-tauri/src/commands/llm.rs`
- [x] 2.2 Thread cache-hit token counts through `LLMUsage`/`LLMResponse` so they reach the frontend `LLMResponse.usage` shape in `src/api/llm/index.ts`. **Scope note:** the proposal's "cost estimator" framing assumed a live per-message cost calculator existed; investigation during implementation found none does — `ModelPricing.cache_read` is only ever used to render reference pricing in the model picker (`LLMProviderSettings.tsx`), not applied against actual response usage anywhere in the app. Implemented as scoped: the raw hit/miss counts are now threaded through end-to-end (Rust → Tauri IPC → browser-mode parity path) for any future consumer, and the DeepSeek models' `cache_read` pricing is populated so the existing model-picker pricing display is accurate today. No new cost-estimation pipeline was invented.
- [x] 2.3 Verify OpenAI/Gemini/Anthropic/Ollama/OpenRouter responses are unaffected (fields remain `None`/absent) — confirmed via `cargo check`/`cargo test --lib commands::llm` (crate has no unit tests for this module; verified via clean compile) and code review of all `LLMUsage` construction sites.

## 3. Frontend: provider type plumbing

- [x] 3.1 Add `"deepseek"` to `ConfiguredLLMProvider` in `src/utils/llmProviderUtils.ts`; confirm `providerRequiresApiKey` returns `true` for it
- [x] 3.2 Add `"deepseek"` to the provider union types in `src/stores/llmProvidersStore.ts` (`LLMProviderConfig['provider']`, `getProvidersByType`) and confirm `syncPrimaryProviderToNativeAI` skips DeepSeek the same way it already skips Gemini
- [x] 3.3 Add a `deepseek` entry to `PROVIDER_INFO` in `src/components/settings/LLMProviderSettings.tsx` (name, icon, default base URL, default model, fallback model list) and include `"deepseek"` in the provider-type picker array. Also updated the mirrored type union in `AIProviderSettings.tsx` (the wrapper that owns `handleAddProvider`/`handleUpdateProvider`).
- [x] 3.4 **Scope corrected** (see 2.2): no existing cost-calc function consumes `cache_read` against real usage, so there was nothing to wire "cache-hit pricing" into. Instead: added `deepseek` to `PROVIDER_CONFIGS` in `src/api/llm/index.ts`, extended `LLMResponse.usage` with `promptCacheHitTokens`/`promptCacheMissTokens`, and additionally extended the provider-union types and DeepSeek routing in `AssistantPanel.tsx`, `QueueScrollPage.tsx`, `RSSScrollMode.tsx`, `visionCapability.ts`, and the PWA/browser-mode backend parity layer (`browser-backend.ts`, including its own `llm_chat`/`llm_get_models` DeepSeek support) — these were not in the original task list but are required for a DeepSeek provider to actually be selectable and usable everywhere providers are chosen, not just in the settings form.

## 4. i18n

- [x] 4.1 **Not applicable, scope corrected during implementation.** Checked how existing providers are localized: none are — `PROVIDER_INFO`/`PROVIDER_CONFIGS` names ("OpenAI", "Google Gemini", etc.) are hardcoded English strings with no `t()` calls and no corresponding keys in any locale file (confirmed `gemini`/`Gemini` do not appear in `en.ts`). Adding DeepSeek-specific i18n keys would have introduced a localization pattern no other provider follows. Left consistent with existing (unlocalized) provider names.

## 5. Verification

- [x] 5.1 Added unit tests in `src/utils/__tests__/llmProviderUtils.test.ts` covering DeepSeek's unconditional API-key requirement (including against a local-looking base URL, since DeepSeek — unlike `openai` — has no keyless/local carve-out)
- [ ] 5.2 Manually configure a DeepSeek provider with a real API key in the running app: verify chat send, streaming, model fetch, and "Test Connection" all succeed — **not done**, requires a live DeepSeek API key which isn't available in this environment. Recommend the user do a quick manual smoke test before relying on this in production.
- [ ] 5.3 Manually verify a multi-turn conversation against DeepSeek shows non-zero cache-hit tokens on the second+ request — **not done**, same reason as 5.2.
- [x] 5.4 Ran `cargo check` and `cargo test --lib commands::llm` (clean, no tests defined for this module) for the Rust side; ran the frontend suite for all touched/related test files (`llmProviderUtils.test.ts`, `useTTS.providerSwitch.test.tsx`, `sync.encryptedProvider.test.ts`) — all pass. Ran `tsc --noEmit` across the whole frontend — no type errors.
