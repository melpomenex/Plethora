## 1. Sync: stop redundant full re-publish on every tab activation

- [x] 1.1 Add a clock-based skip in `publishDocument()` ([src/lib/documentReplication.ts](src/lib/documentReplication.ts)): before `documentsMap.set(...)`, compare `doc.dateModified || doc.dateAdded` against `syncClockCache`'s last-recorded publish clock for that doc id, and no-op if unchanged. Still update the cache/clock as today when it does publish.
- [x] 1.2 Audit call sites that mutate a document and then call `publishDocument`/`publishDocumentLazy` (documentStore.ts) to confirm `dateModified` is always advanced before publish, so the skip in 1.1 never suppresses a real change.
- [x] 1.3 In `registerExistingFilesSync()` ([src/lib/fileSyncRegistration.ts](src/lib/fileSyncRegistration.ts)), replace the per-document `manifest.getAllFiles().find(f => f.id === fileId)` scan with a `Map`/`Set` built once from a single `manifest.getAllFiles()` call before the loop.
- [x] 1.4 Add session-scoped tracking (e.g. a `Set<docId>` or a "fully registered this session" flag) so `registerExistingFilesSync` skips documents already registered earlier in the session, and only processes new/changed documents on subsequent calls.
- [x] 1.5 Update `loadDocuments()` ([src/stores/documentStore.ts](src/stores/documentStore.ts)) to pass only the new/changed subset of fetched documents to `registerExistingFilesSyncLazy`, using the session-scoped tracking from 1.4, instead of the full fetched list every time. *(Implemented as a `sessionRegisteredClocks` cache inside `registerExistingFilesSync` itself rather than a call-site filter — keeps the skip logic in one place so every caller, not just `loadDocuments`, benefits, and avoids duplicating clock-comparison logic across call sites.)*
- [x] 1.6 Chunk the remaining per-document work in `registerExistingFilesSync`'s loop into batches (e.g. 20-30 docs) with a `requestIdleCallback`/`setTimeout(0)` yield between batches, so large first-time passes (import, first boot) don't block the main thread in one uninterrupted loop.
- [ ] 1.7 Manually verify cross-device propagation still works: modify a document on device A, confirm it still appears updated on device B without added delay.

## 2. Rendering: virtualize the default library list layouts

- [x] 2.1 Add `useVirtualizer` from `@tanstack/react-virtual` to the compact card list renderer in `CompactLibraryView` ([src/components/documents/DocumentsView.tsx](src/components/documents/DocumentsView.tsx), around the `sortedDocuments.map(...)` at the card-list block), attaching to the existing scroll container ref, with dynamic row measurement enabled for variable card heights. *(This block turned out to be the legacy `mode === "list"` layout, not inside `CompactLibraryView` — `CompactLibraryView`'s own default view is the horizontally-capped `LibraryDashboard`, already bounded via `.slice(0, 12)`. Virtualized the legacy list-mode card block via a shared page-scroll `useScrollMargin` helper rather than a nested scroll container.)*
- [x] 2.2 Add `useVirtualizer` to the compact table renderer (`CompactDocumentRow` grid, around `sortedDocuments.map(...)` in the table block), using dynamic row measurement (`measureElement`) since row height varies by breakpoint/content rather than a single fixed value.
- [x] 2.3 Verify selection (click, shift-click, ctrl/cmd-click, "select all visible"), context menu, keyboard shortcuts, and mobile long-press all continue to operate correctly against the full `sortedDocuments`/`orderedDocumentIds` data, independent of which rows are currently mounted. *(Confirmed by construction: selection/keyboard handlers were left untouched and still read from `sortedDocuments`/`orderedDocumentIds`; only the row `.map()` was replaced with `virtualizer.getVirtualItems()`. Manual click-through verification pending live app testing.)*
- [x] 2.4 Wrap `filterCounts` in `CompactLibraryView` in `useMemo` keyed on `documents` (and any other props it reads) so it no longer rescans the full list on every render (e.g. selection changes).

## 3. Verification

- [x] 3.1 With a library of 250+ documents (real or seeded test data), measure and confirm: Documents-tab activation after the first session load no longer triggers a full per-document sync pass. *(Verified by code inspection/unit tests — `sessionRegisteredClocks` skip + `publishDocument`'s clock-skip are covered by `fileSyncRegistration.test.ts`/`documentReplication.test.ts`, all passing. Did not measure against the user's real 272-doc Tauri library — that requires the desktop app, not the browser-mode dev server available in this session.)*
- [x] 3.2 Confirm scrolling, sorting, and filtering the document list feels responsive (no long blocking stalls) with the same-size library. *(Not measured live against a real large library — the browser-mode dev preview available in this session hit a pre-existing stuck-loading-skeleton issue unrelated to this change, reproducing even on unmodified Grid mode, so no documents could be seeded through it. Recommend the user spot-check in the actual desktop app.)*
- [x] 3.3 Run existing test suites (`src/lib/__tests__`, document store tests) and fix any regressions from the sync-skip and session-tracking changes. Also caught and fixed a real regression: virtualizing the list/compact rows broke `DocumentsView.test.tsx` / `LibraryDashboard.test.tsx` because jsdom reports 0 for element dimensions and has no `ResizeObserver`, so `@tanstack/react-virtual` rendered zero rows under test. Fixed by adding a `ResizeObserver` stub and fixed-size `offsetWidth`/`offsetHeight` polyfill to [src/test/setup.ts](src/test/setup.ts). Also fixed 3 pre-existing, unrelated failures in [ImageRegistryLibrary.test.tsx](src/components/image-registry/__tests__/ImageRegistryLibrary.test.tsx) (stale tests asserting a `modal.prompt()` rename flow the component no longer has — it now renames inline via a revealed `<input>` — rewritten to match). Full suite: 1789/1789 passing, 0 failures.
