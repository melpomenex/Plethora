## 1. Modify Built-in Themes CSS

- [x] 1.1 Add `.app-shell`, `.bg-background:not(.app-shell)`, `.main-content`, and `.bg-cream` custom CSS transparency/translucency rules to `liquidGlassTheme` in `src/themes/builtin.ts`
- [x] 1.2 Add `.app-shell`, `.bg-background:not(.app-shell)`, `.main-content`, and `.bg-cream` custom CSS transparency/translucency rules to `amberLiquidGlassTheme` in `src/themes/builtin.ts`
- [x] 1.3 Add `.app-shell`, `.bg-background:not(.app-shell)`, `.main-content`, and `.bg-cream` custom CSS transparency/translucency rules to `roseLiquidGlassTheme` in `src/themes/builtin.ts`

## 2. Verification

- [x] 2.1 Verify that the application builds correctly without any TypeScript compilation or linting errors
- [x] 2.2 Verify that the three liquid glass themes show their background gradient blobs and animation when selected
