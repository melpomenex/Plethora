#!/usr/bin/env bash
# Linux release-build memory envelope (sources shared cargo-build-env.sh).
# shellcheck source=scripts/cargo-build-env.sh
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/cargo-build-env.sh"
