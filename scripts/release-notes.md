### Added
- side-by-side thumbnail rendering on all screens in RSS Scroll Mode.

### Fixed & Improved
- Optimized the RSS Scroll Mode view on mobile devices to reclaim significant vertical screen real estate.
- Reduced title font sizes (`text-base` to `text-lg` instead of `text-2xl` on mobile) and adjusted line-height to fit headlines in fewer lines.
- Compacted padding on mobile action buttons (`Show cover image`, `View Full Content`), categories list, feed indicator, and footer buttons.
- Re-adjusted header padding to prevent overlay collision with absolute top navigation controls.
